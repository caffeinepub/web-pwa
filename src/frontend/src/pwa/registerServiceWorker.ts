// Service worker registration disabled for website-first delivery
export function registerServiceWorker(): void {
  // No service worker registration - this is a standard website, not a PWA
  return;
}
